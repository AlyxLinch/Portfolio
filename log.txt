2026-07-25
- User requested a new test page on the domain for a graph experiment.
- Added development log file to track requests and implementation notes for this testing work.
- Planned a standalone /graph-test/ page with a framed graph, live logarithmic function label, axis scale slider, and coefficient slider.
- Implemented /graph-test/ with HTML, CSS, and canvas JavaScript.
- Function is y = -a · ln(x + 1), drawn for x >= 0 so the curve lives in quadrant 4 below the x-axis.
- Added one slider for congruent x/y axis scale and one slider for coefficient a; the displayed function updates live.
- Visual QA found the equation label too dark and wide on mobile; changed it to the yellow gradient treatment and added responsive sizing.
- Moved the graph scale annotation away from the axis line for cleaner readability.
- Mobile QA found horizontal overflow from the graph/equation layout; clamped the graph shell/card/canvas and reduced compact equation sizing.
- Removed the in-graph scale annotation on compact canvas widths because the slider output already shows the scale.
- Adjusted mobile graph frame to a square-ish viewport with smaller plot padding so the graph fits better on phones.
- Removed the compact in-canvas "Quadrant IV" label because it crowded the right edge; the page title already identifies the quadrant.
- User requested a one-screen graph test with no scrolling, a reciprocal function, equal x/y axis lengths, no grid lines or tick marks, and a quadrant toggle that reflects the function into whichever quadrant is selected.
- Reworked the graph test around y = a / x, with sign reflection for quadrants I-IV so the reciprocal curve is always visible in the chosen quadrant.
- Updated the layout to size from viewport height, keep the graph square, and keep the controls/header within one screen.
- Tightened the narrow-screen sizing so the graph panel stays square while fitting beside the full control stack without page scrolling.
2026-07-25
- Replaced the reciprocal quadrant graph with a complete parametric squircle.
- Chose a lightweight Canvas 2D superellipse renderer: |x/r|^n + |y/r|^n = 1.
- Reworked the coefficient slider into a squircle exponent control and removed the quadrant controls and blue reference point.
- Closed the sampled contour explicitly so the full graph renders without clipped or missing sections.
- Removed the remaining axes, numbers, and scale control to make the canvas a pure squircle visualization.
- Added editable starting exponent and duration fields, an ending exponent slider, and a play/pause control.
- The animation now advances through a fixed 60 exponent steps per second and completes in the selected duration (four seconds by default).
- Changed the squircle from a glowing outline to a solid fill with no shadow.
- Replaced the ending exponent slider with a cached display-aware calculation based on canvas size and device pixel ratio.
- The terminal exponent is solved where the curve reaches the center of the corner pixel; it is recalculated only when the rendered screen size or pixel density changes.
- Replaced the starting exponent number field with a live value slider.
- Changed the animation from linear addition to geometric interpolation, giving every 60 Hz step the same proportional exponent increase.
- Added a custom interpolation-style dropdown using the portfolio design system.
- Named the existing geometric progression "Linear" because it reads as visually linear against the squircle function, with additional cubic ease-in, ease-out, and ease-in-out timing options.
- Changed playback into a continuous boomerang loop that retraces the selected interpolation curve in reverse.
- Added fixed two-second holds at both the starting and ending exponent while preserving pause/resume and the selected travel duration for each direction.
- Expanded the canvas into four synchronized squircles: square, 4:3 landscape, 16:9 landscape, and 3:4 portrait.
- Updated the terminal exponent calculation to use the largest physical radius across the full shape set.
- During the ending hold, the sampled squircles are replaced with exact canvas rectangles so every edge and corner is fully crisp.
- Snapped the exact endpoint rectangle bounds to the physical device-pixel grid to avoid antialiased half-pixel edges.
- Converted the four canvas specimens into real HTML buttons with responsive SVG superellipse surfaces and usable text labels.
- Each button now animates forward once on hover or keyboard focus, holds as a crisp rectangle, and reverses to its starting squircle when hover/focus leaves.
- Added varied button treatments including solid fills, translucent fill, and 1px outline/accent borders while preserving the four aspect-ratio studies.
- Replaced stretched full-rectangle superellipses with a corner-grafting model built from four copies of one square superellipse quadrant.
- The four undistorted corner curves are now joined by true straight edges, and terminal exponent calculations use the shared square corner radius instead of the rectangle's long side.
- Reserved a fixed two-line height for the changing equation/title text so the button frame no longer shifts as hover states update the copy.
